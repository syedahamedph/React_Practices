import React from "react";

const PyFunctions = () => (
  <div>
    <h3>Functions</h3>
    <p>
      Functions are blocks of reusable code. Use `def` to define a function.
      Python also supports lambda (anonymous) functions.
    </p>
    <a
      href="https://www.youtube.com/watch?v=9Os0o3wzS_I"
      target="_blank"
      rel="noopener noreferrer"
    >
      Watch Python Functions Tutorial
    </a>
  </div>
);

export default PyFunctions;
